ktor:
    application:
        modules:
            - com.example.ApplicationKt.module
    deployment:
        port: 8080
        watch:
            - classes
            - resources

    logging:
        level: TRACE

    environment: development



db:
    url: "jdbc:postgresql://localhost:5432/db_group_chat"
    driver: "org.postgresql.Driver"
    username: "postgres"
    password: "noibla250602"
    maxPoolSize: 5

